# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Insufficient Top Spacing Across Viewports
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate insufficient spacing exists
  - **Scoped PBT Approach**: Scope the property to concrete viewport sizes (mobile: 375px, tablet: 768px, desktop: 1440px) to ensure reproducibility
  - Test that #projectsPanel top padding is insufficient across viewports (from Bug Condition in design)
  - The test assertions should match the Expected Behavior Properties from design:
    - Mobile (< 640px): top padding should be 12-16px
    - Tablet (640px-1023px): top padding should be 16-20px
    - Desktop (≥ 1024px): top padding should be 16-24px
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - Mobile: measures 12px (borderline acceptable but could be improved)
    - Tablet: measures 16px (borderline acceptable)
    - Desktop: measures 16px (insufficient - should be 16-24px)
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Panel Styling and Functionality Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-spacing properties:
    - #projectsPanel has border-radius: 18px (16px on mobile)
    - #projectsPanel has border: 1px solid borderDim color
    - #projectsPanel has background: bgPanel color
    - Side padding (left/right): 12px mobile, 16px tablet/desktop
    - Bottom padding: 12px mobile, 16px tablet/desktop
    - Child elements (toolbar, search, sort, grid) render correctly
    - Other panels (#certPanel, #achPanel, etc.) maintain their spacing
    - Theme switching doesn't affect spacing values
    - Page zoom transformations apply correctly
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for insufficient #projectsPanel top spacing

  - [x] 3.1 Implement the fix
    - Open public/index.html
    - Locate the #projectsPanel element (around line 723)
    - Replace `p-3 sm:p-4` with differentiated padding classes
    - **Recommended Option A** (more generous spacing):
      - Replace with: `pt-4 px-3 pb-3 sm:pt-5 sm:px-4 sm:pb-4 lg:pt-6`
      - Mobile: 16px top, 12px sides/bottom
      - Tablet: 20px top, 16px sides/bottom
      - Desktop: 24px top, 16px sides/bottom
    - **Alternative Option B** (conservative adjustment):
      - Replace with: `pt-3 px-3 pb-3 sm:pt-4 sm:px-4 sm:pb-4 lg:pt-5`
      - Mobile: 12px top, 12px sides/bottom
      - Tablet: 16px top, 16px sides/bottom
      - Desktop: 20px top, 16px sides/bottom
    - No other changes required - compact-panel class handles border-radius
    - _Bug_Condition: isBugCondition(input) where input.element == "#projectsPanel" AND input.topPadding < expectedMinimumSpacing(input.viewport)_
    - _Expected_Behavior: expectedBehavior(result) where result.topPadding >= expectedMinimumSpacing(viewport) for mobile: 12-16px, tablet: 16-20px, desktop: 16-24px_
    - _Preservation: All styling and functionality NOT related to top padding must remain unchanged (rounded corners, borders, backgrounds, child layouts, other sections, theme consistency, zoom behavior)_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Proper Responsive Top Spacing
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify top padding measurements:
      - Mobile (< 640px): 12-16px ✓
      - Tablet (640px-1023px): 16-20px ✓
      - Desktop (≥ 1024px): 16-24px ✓
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - Panel Styling and Functionality Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all styling and functionality preserved:
      - Rounded corners unchanged ✓
      - Border and background unchanged ✓
      - Side/bottom padding unchanged ✓
      - Child element layouts unchanged ✓
      - Other sections spacing unchanged ✓
      - Theme switching consistent ✓
      - Zoom behavior correct ✓

- [x] 4. Checkpoint - Ensure all tests pass
  - Verify bug condition test passes (proper responsive spacing achieved)
  - Verify preservation tests pass (no regressions to styling/functionality)
  - Visually inspect #projectsPanel across mobile, tablet, and desktop viewports
  - Compare spacing to Google search result standards
  - Ask the user if questions arise
